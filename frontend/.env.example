# Local development: runs the app against the local backend
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
#
# Production (Vercel): set in the Vercel project env vars instead — do NOT commit
# NEXT_PUBLIC_API_URL=https://hrms-9ypm.onrender.com/api
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name